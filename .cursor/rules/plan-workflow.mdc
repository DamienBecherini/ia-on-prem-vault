---
description: Plan creation workflow for this vault
alwaysApply: false
---

# Plan Workflow

When the user asks to create a plan:

- Save the plan in `docs/plans/` of the repository that owns the requested work.
- If `docs/plans/` does not exist, create it in that repository.
- Name the file `YYYY_MM_DD_HH-MM_<branch-slug>_<plan-title>.plan.md`.
- Use local current time for `YYYY_MM_DD_HH-MM`.
- Build `<branch-slug>` from the current git branch by replacing `/` with `__`.
- Normalize `<plan-title>` to lowercase ASCII kebab-case.
- Include the goal, scope, files to create/modify, execution tasks, and validation steps.
- Write plan content in English.
- If the user asks to execute the plan, append a final implementation/build report to the same plan file with changed files, validation evidence, and limitations.

## PR Delivery (default for all executions)

When executing a plan, follow the git workflow in `git-workflow.mdc`:

1. **Create a branch** before making any file changes: `<type>/<plan-title-slug>` (derive type from the plan: `feat`, `fix`, `chore`, or `docs`).
2. **Apply all file changes** on that branch.
3. **Commit** with: `<type>(<scope>): <plan title in imperative>`.
4. **Push** to origin.
5. **Create a PR** using the template from `git-workflow.mdc`. Include the plan filename in the PR body.
6. **Report the PR URL** in chat.

Exception: if the user says "commit to main" or "push directly", skip the branch and PR steps.

