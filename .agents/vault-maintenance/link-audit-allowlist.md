# Link audit allowlist

Agent maintenance file — not reader-facing content.

Lists wiki-links that intentionally target pages not yet published. Used by `npm run audit:links` in the engine repo so CI fails only on unexpected broken links.

## Prefixes

- 03-stack-logicielle/
- 04-blueprints/

## Slugs

- 03-stack-logicielle/rag-et-agents-openhuman
